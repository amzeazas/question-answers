<li>
  {{#linkTo 'question' question.id}}{{question.text}}{{/linkTo}} by {{question.author}}
  {{update-question question=question update="update"}}
  <button class="btn btn-danger btn-xs"{{action 'delete' question}}>Delete</button>
</li>
